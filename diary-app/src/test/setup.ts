import '@testing-library/jest-dom'
// Note: cleanup from @testing-library/react is only needed for component tests
// Utility tests don't need it, but we keep the import here for future component tests
